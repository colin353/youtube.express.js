// Generated by CoffeeScript 1.6.2
var embedYoutube, onYouTubePlayerReady, video_not_yet_started;

embedYoutube = function() {
  return swfobject.embedSWF("http://www.youtube.com/apiplayer?enablejsapi=1&playerapiid=ytplayer&version=3", "ytplayer", size.x, size.y, "8", null, null, {
    allowScriptAccess: 'always'
  }, {
    id: playerID
  });
};

$(function() {
  return embedYoutube();
});

onYouTubePlayerReady = function() {
  document.player = $('#' + playerID).get(0);
  document.player.playVideo();
  return document.connectToServer();
};

document.didSkipVideo = function(video_code) {
  return document.player.loadVideoById(video_code);
};

document.didLoadVideo = function(video_code) {
  return document.player.loadVideoById(video_code);
};

document.didSetVolume = function(vol) {
  return document.player.setVolume(vol);
};

document.didPlayVideo = function() {
  return document.player.playVideo();
};

document.didPauseVideo = function() {
  return document.player.pauseVideo();
};

video_not_yet_started = function() {
  switch (document.player.getPlayerState()) {
    case 1:
    case 2:
    case 3:
    case 5:
      return false;
    default:
      return true;
  }
};
